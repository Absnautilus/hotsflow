VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Required: apps/guest is deployed once per hotel, not once for the whole
# platform (guests never pick a property, they scan a QR code for their own
# room) -- this is the legacy hotels.id for the one hotel this deployment
# serves, referenced by guest_login/fetchMenu.
VITE_HOTEL_ID=

# Optional: shows a "sei dello staff?" link on the login screen pointing at
# the staff shell's real URL. Omit to hide the link entirely.
VITE_STAFF_APP_URL=

# Optional: enables push notifications elsewhere in the platform; unused by
# the guest screens themselves today, kept for parity with apps/web/modules/housekeeping's .env.example.
VITE_VAPID_PUBLIC_KEY=
