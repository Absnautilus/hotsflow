VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: enables push notifications for the Housekeeping on-duty toggle.
# The toggle hides itself when unset -- the app runs fine without it.
VITE_VAPID_PUBLIC_KEY=
